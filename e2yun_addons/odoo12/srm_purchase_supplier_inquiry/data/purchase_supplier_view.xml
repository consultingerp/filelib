<?xml version="1.0"?>
<odoo>
    <data>
        <record id="action_p_inquiry_redirect" model="ir.actions.server">
           <field name="name">purhcase confim forward</field>
            <field name="condition">True</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="code">action = env['purchase.inquiry.redirect'].inquiry_redirect()</field>
            <field name="state">code</field>
            <field name="type">ir.actions.server</field>
        </record>
    </data>
</odoo>
